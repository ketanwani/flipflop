from setuptools import setup, find_packages

setup(
    name='flipflop-proxy',
    version='0.1.0',
    description='Reverse proxy library for python Django',
    author='Ketan Wani',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here if any
    ],
)
